# 開發進度（地基已建，待續）

> 記錄時間：2026-05-24。搭配 [開發分工與接口規格.md](開發分工與接口規格.md)（API 契約 + 五人分工）一起看。

## ✅ 已完成：後端「地基」骨架（讓五人可並行、互不改同檔）

- **共用層**
  - `backend/app/utils/responses.py`：統一回應 `ok(data, **extra)` / `err(message, code)`（成功 `{status:"success",data}`、失敗 `{status:"error",message}`）。
  - `backend/app/utils/auth.py`：`line_required`（讀 `X-Line-User-Id`）、`admin_required`（讀 `X-Admin-User`）裝飾器 + `find_user()`。**簡易版**，已可運作。
- **設定**：`backend/config.py` / `example_config.py` 移除 `SQLALCHEMY_*`，新增 `GOOGLE_MAPS_API_KEY`、`LINE_CHANNEL_ID`。DB 名稱已對齊 `garbage_database`。
- **API 骨架**（端點齊全、回應統一、實作處標 `# TODO(Px)`）：
  - `api/routes.py`（stations：`/search`、`/<id>`、`/<id>/next`、`/by-route/<id>`）
  - `api/favorites.py`（GET/POST/PATCH/DELETE）← 已有組員開始填 PATCH 實作
  - `api/notifications.py`（GET/POST/PATCH/DELETE）
  - `api/info.py`（`/bag-regulations`、`/bulky-waste`、`/announcements`）
  - `api/admin.py`（`/login`、`/users`、`/announcements`(+`/push`)、`/sync-log`）
  - `api/users.py`：已新增 `GET /me`（既有 `register` 保留）
  - `api/pages.py`：LIFF 通用路由 `GET /liff/<page>` + 範本 `templates/liff/index.html`
- **`app/__init__.py`**：一次註冊全部 8 個 Blueprint（之後新增端點不必再動本檔）。

## ✅ 煙霧測試（已完成）
- 語法檢查：24 個檔 **0 錯誤**。
- `create_app()` 載入：程式碼本身 OK，唯一卡點是 **MySQL 未啟動**（`db.py` 在 import 時建連線池）。啟動 XAMPP/MySQL（DB 名 `garbage_database`）後 `python -c "from app import create_app; create_app()"` 即可通過。

## ⏳ 尚未完成（接著做 — 交由五人團隊依規格書分工）
2. **各 API 的 `# TODO(Px)` 填實作**（依規格書契約與五人分工）。
3. **服務層空檔**：`services/geo_service.py`（P1）、`services/line_service.py`（P3）。
4. **背景任務（M4，未動）**：`tasks/notifier.py`、`tasks/data_sync.py`、整併兩份 `newimport.py`。
5. **`backend/create_admin.py`（未建）**：建首位管理員，否則 admin `/login` 無帳號可測。
6. **前端**：React 管理後台（`frontend/` 仍是 CRA 樣板，P4）、LIFF 功能頁（`templates/liff/*.html`，P5）。

## 提醒
- 簡易版身分：使用者帶 `X-Line-User-Id`、管理員帶 `X-Admin-User`；安全版（LIFF ID Token / JWT）見規格書第 6 節「以後做」。
- 新增 LIFF 頁＝在 `templates/liff/` 丟一支 `.html`，由 `/liff/<檔名>` 取用，不必改 Python。
